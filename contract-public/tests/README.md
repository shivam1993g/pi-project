# Public tests

The starter validates the stable execution contract, telemetry reconciliation, output reset safety, and neutral app build in `test/`.

Participants should add development-prompt browser tests here or in their solution. The official evaluator runs separate tests from outside the submitted repository.
